<% @posts.each do |post| %>
  <div class="post-list">
    <h2 class="post-list__title"><%= link_to post.title, post %></h2>
    <p class="post-list__date"><%= post.created_at.strftime("%B, %d, %Y") %></p>
  </div>
<% end %>
